jwt:
  secret: UMGWByE8Ja/FyDFLqqOnKCN4GiFd+cm01UQnk+HTZjYAOUxTu7tEMyfXTBePrxQ4wNDfcmGymX0KgnS/9FGKvA==

spring:
  application:
    name: taskmanager-app
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/taskdb
    username: postgres
    password: 

  jpa:
    hibernate:
      ddl-auto: none
      naming:
         physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
         current_session_context_class: org.springframework.orm.hibernate5.SpringSessionContext
         dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoints:
    health:
      probes:
        enabled: true
    web:
      exposure:
        include:
          - "health"
          - "info"
          - "metrics"
          - "env"